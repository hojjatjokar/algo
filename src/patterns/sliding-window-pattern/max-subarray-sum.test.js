import { expect, test } from '@jest/globals';
import maxSubarraySum from './max-subarray-sum';

test('find max subarray sum', () => {
  expect(maxSubarraySum([1, 2, 5, 2, 8, 1, 5], 2)).toBe(10);
});

test('find max subarray sum', () => {
  expect(maxSubarraySum([4, 2, 1, 6], 1)).toBe(6);
});

test('find max subarray sum', () => {
  expect(maxSubarraySum([1, 2, 5, 2, 8, 1, 5], 2)).toBe(10);
});

test('find max subarray sum', () => {
  expect(maxSubarraySum([1, 2, 5, 2, 8, 1, 5], 2)).toBe(10);
});
